With Atomik running:

```sh
$ gradle build
$ gradle run
```

That will generate some data, upload the required templates, create an EHR and commit the compositions.

TODO: change the base composition because it has some missing nodes.